<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="SimpleList.Views.ConvertFileFormatView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:SimpleList.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DataContext="{x:Bind vm:ConvertFileFormatViewModel}"
    x:Uid="ConvertFileFormatView"
    DefaultButton="Primary"
    >

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <TextBlock Margin="0 0 0 10" TextWrapping="Wrap">
            <Run x:Uid="ConvertFileFormatView_SupportedFormats" />
            <Run Text="{Binding FormattedExtensions}" />
        </TextBlock>

        <RelativePanel Grid.Row="1">
            <TextBlock x:Uid="ConvertFileFormatView_TargetFormat" FontWeight="Bold" RelativePanel.AlignLeftWithPanel="True" />
            <ComboBox ItemsSource="{Binding TargetFormats}" SelectedIndex="0" SelectedItem="{Binding SelectedFormat, Mode=TwoWay}"  RelativePanel.AlignRightWithPanel="True" />
        </RelativePanel>

        <RelativePanel Grid.Row="2">
            <TextBlock x:Uid="ConvertFileFormatView_SavePath" FontWeight="Bold" RelativePanel.AlignLeftWithPanel="True" />
            <StackPanel Orientation="Horizontal"  RelativePanel.AlignRightWithPanel="True">
                <TextBox Text="{Binding SavedFilePath}" IsEnabled="False" />
                <Button x:Uid="ConvertFileFormatView_Browse" Command="{Binding ConvertFileFormatCommand}" />
            </StackPanel>
        </RelativePanel>
    </Grid>
</ContentDialog>
