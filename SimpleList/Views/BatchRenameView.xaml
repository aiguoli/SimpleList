<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="SimpleList.Views.BatchRenameView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:SimpleList.ViewModels"
    mc:Ignorable="d"
    d:DataContext="{x:Bind vm:BatchRenameViewModel}"
    x:Uid="BatchRenameView"
    PrimaryButtonCommand="{Binding RenameFilesCommand}"
    DefaultButton="Primary">

    <StackPanel Width="520" Spacing="12">
        <ComboBox x:Uid="BatchRenameView_Mode" SelectedIndex="{Binding ModeIndex, Mode=TwoWay}" HorizontalAlignment="Stretch">
            <ComboBoxItem x:Uid="BatchRenameView_Mode_String" />
            <ComboBoxItem x:Uid="BatchRenameView_Mode_Regex" />
        </ComboBox>
        <TextBox x:Uid="BatchRenameView_Find" Text="{Binding FindText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        <TextBox x:Uid="BatchRenameView_Replace" Text="{Binding ReplaceText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        <TextBlock
            Foreground="{ThemeResource SystemFillColorCriticalBrush}"
            Text="{Binding ErrorMessage}"
            TextWrapping="Wrap" />
        <ListView
            MaxHeight="260"
            ItemsSource="{Binding PreviewItems}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="vm:BatchRenamePreviewItem">
                    <Grid ColumnSpacing="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{x:Bind OriginalName}" TextTrimming="CharacterEllipsis" />
                        <FontIcon Grid.Column="1" FontSize="12" Glyph="&#xE72A;" />
                        <TextBlock Grid.Column="2" Text="{x:Bind NewName}" TextTrimming="CharacterEllipsis" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackPanel>
</ContentDialog>
