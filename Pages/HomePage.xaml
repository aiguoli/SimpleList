<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="SimpleList.Pages.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converter="using:SimpleList.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <converter:FileSizeConverter x:Key="FileSizeConverter" />
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" FontSize="25" Margin="20" Text="Hello, Guest" x:Name="GreetingText" />
        <TextBlock Grid.Row="1" x:Name="StorageText" Margin="20 0 0 20" FontSize="15" />
        <StackPanel Orientation="Vertical" Grid.Row="2" Margin="20 0 20 0">
            <TextBlock Text="Quick Start" FontSize="25" />
            <TextBlock 
                Text="Click on Files in the left navigation menu, then click the refresh button (or press F5), log in to OneDrive, and list the root directory files."
                FontSize="16"
                Margin="0 20 0 5" />
        </StackPanel>
    </Grid>
</Page>
