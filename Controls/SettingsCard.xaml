<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="SimpleList.Controls.SettingsCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SimpleList.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Grid.Row="1" Orientation="Vertical" Margin="30 0 30 0">
        <Grid BorderBrush="DarkGray" BorderThickness="1" Padding="10 15 10 15" CornerRadius="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ContentControl Grid.Column="0" Content="{x:Bind Icon, Mode=OneWay}" />
            <StackPanel Grid.Column="1" Orientation="Vertical" Margin="15 0 15 0">
                <TextBlock Text="{x:Bind Title}" />
                <TextBlock Text="{x:Bind Description}" />
            </StackPanel>
            <ContentControl Grid.Column="2" Content="{x:Bind Content, Mode=OneWay}" />
        </Grid>
    </StackPanel>

</UserControl>
